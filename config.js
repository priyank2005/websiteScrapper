var config={};
config.requestThreshold=2;
config.parseEntireWebsite=true;
config.stayOnDomain=true;//This Applies to only external webpages, Your CDN assets would still be downloaded;
module.exports=config;
